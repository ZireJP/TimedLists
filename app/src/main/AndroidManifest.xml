<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mytimer.julianpeters.xyz.timedlists">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListActivity" />

        <provider
            android:name=".providers.ListsContentProvider"
            android:authorities="mytimer.julianpeters.xyz.timedlists.providers.ListsContentProvider"></provider>

        <activity android:name=".ItemActivity"></activity>
        <activity android:name=".Popup"
            android:theme="@style/AppTheme.PopUpTransparent">
        </activity>
        <activity android:name=".RunPopUp"
            android:theme="@style/AppTheme.PopUpTransparent">
        </activity>
    </application>

</manifest>