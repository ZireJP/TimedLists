<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mytimer.julianpeters.xyz.timedlists">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name=".providers.ListsContentProvider"
            android:authorities="mytimer.julianpeters.xyz.timedlists.providers.ListsContentProvider" />

        <activity
            android:name=".activities.main.ListActivityMain"
            android:theme="@style/AppTheme.NoAnimation"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.main.ItemActivity"
            android:theme="@style/AppTheme.NoAnimation"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".activities.main.ListActivitySub"
            android:theme="@style/AppTheme.NoAnimation"/>
        <activity
            android:name=".activities.popup.SetRepeatPopup"
            android:theme="@style/AppTheme.PopUpTransparent"/>
        <activity
            android:name=".activities.main.RunActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:windowSoftInputMode="adjustNothing"/>
        <activity
            android:name=".activities.popup.NotePopUp"
            android:theme="@style/AppTheme.PopUpTransparent"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".activities.popup.SetTimePopUp"
            android:theme="@style/AppTheme.PopUpTransparent"/>

    </application>

</manifest>